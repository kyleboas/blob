{
  "name": "blob",
  "version": "1.0.0",
  "scripts": {
    "deploy": "wrangler deploy -c wrangler.agent.toml",
    "deploy:sandbox": "wrangler deploy -c wrangler.sandbox.toml",
    "test": "tsx --test src/tests/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "tsx": "^4.20.3",
    "typescript": "^5.9.3",
    "wrangler": "^4.69.0"
  },
  "dependencies": {
    "@cloudflare/sandbox": "^0.7.8"
  }
}
