{
  "name": "blob-agent",
  "version": "1.0.0",
  "description": "Cloudflare Worker migration for Blob agent",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "tsc --noEmit",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "deploy:dry-run": "wrangler deploy --dry-run"
  },
  "dependencies": {
    "@cloudflare/agents": "^0.0.20",
    "@cloudflare/sandbox": "^0.1.0",
    "wrangler": "^4.34.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.41",
    "@cloudflare/workers-types": "^4.20260129.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
