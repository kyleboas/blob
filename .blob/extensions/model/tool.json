{
  "name": "model",
  "description": "Budget-aware model picker. Goal: under $20/month. Default: Cloudflare (cheap). Escalate: Anthropic (expensive, use sparingly).",
  "input_schema": {
    "type": "object",
    "properties": {
      "command": {
        "type": "string",
        "enum": ["budget", "pick", "escalate", "status"]
      },
      "task": { "type": "string" }
    },
    "required": ["command"]
  }
}
