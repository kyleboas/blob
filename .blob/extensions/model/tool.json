{
  "name": "model",
  "description": "Switch LLM model. Default is routine (Mistral 7B). Use escalate for hard tasks, simple for chat only.",
  "input_schema": {
    "type": "object",
    "properties": {
      "command": {
        "type": "string",
        "enum": ["current", "escalate", "simple", "list"]
      }
    },
    "required": ["command"]
  }
}
