name = "blob"
main = "src/index.ts"
compatibility_date = "2024-02-08"

[triggers]
crons = ["*/5 * * * *"]

# KV namespace must be configured in Cloudflare dashboard
# or via wrangler kv:namespace create CONFIG
# then bind it in the dashboard under Settings > Variables > KV Namespace Bindings
